version: 0.2

cache:
  paths:
    - node_modules/**/*

env:
  shell: bash
  secrets-manager:
    GITHUB_ACCESS_TOKEN: GITHUB_PERSONAL_ACCESS_TOKEN:value

phases:
    install:
      runtime-versions:
        nodejs: 16
      on-failure: ABORT
      commands:
        - |
          chmod +x scripts/pullRequest/install
          bash scripts/pullRequest/install
    build:
      on-failure: ABORT
      commands:
        - |
          npm run build:home
